name: deploy-cloud

# Устарело — основной pipeline: .github/workflows/deploy.yml
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use workflow deploy.yml on push to main"
